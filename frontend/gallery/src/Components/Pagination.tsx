// import "./maincontent.css";
import { Pagination } from "antd";

function MyPagination() {
  return <Pagination className="pagination" defaultCurrent={1} total={50} />;
}

export default MyPagination;
